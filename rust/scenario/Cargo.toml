[package]
name = "scenario"
version = "0.1.0"
edition = "2021"

[dependencies]
sim-core = { path = "../sim-core" }
assets = { path = "../assets" }
sim = { path = "../sim" }
render = { path = "../render" }
